package manager.task.common.utils

/**
 * TODO : (Done) WS механизм
 * TODO : (Done) отправка ответа Только когда ответ есть.
 * TODO : (Done) авто-парсинг JSON в POJO ожидаемый методов который обрабатывает path
 * TODO : (Done) wrap bad gson ParseException
 * TODO : (Done) check try to add path duplicate - Exception if it's duplicate!
 * TODO : (Done) test parse primitive types ? -> *magic* valueWrapper ?
 * TODO : (Done) pathCtx.currentUser by lazy {if(!isNeedAuth) thrown AuthException}
 * TODO : (Done) printTable java -> Kotlin
 * TODO :
 * TODO : ### path ###
 * TODO : (Done) Unit param - WORK
 * TODO : (Done) One pram - WORK
 * TODO : (Done) Ctx + param - WORK
 * TODO : (Done) Ctx + param + auth=false - WORK
 * TODO : (Done) primitive param - WORK
 *
 * TODO : (Wait - общение с front-end) error pojo - единая структура для всех error
 * TODO : (Wait - реальная auth) сделать реальных auth когда уже будет возможность.
 * TODO :
 * TODO : ### ГЛОБАЛЬНО ###
 * TODO : ? postman workspace ?
 * TODO : (Wait) JDBC provider (reactive)
 * TODO : частичный забор данных как в GQL
 * TODO : DTO id -> entity
 * TODO : ? свой аналог кролика/kafka - message hub
 * TODO :
 * TODO : - по готовности WS engine написать Cпецификацию по этому механизму.
 * TODO :
 * TODO :
 * TODO : authService && authServicePoxy - на сколько юзабельный кейс? Заменить 1 бин, на другой.
 * TODO :
 * TODO :

 * TODO : Спецификация по WS.Income
 * TODO : Спецификация по WS.Outcome
 *
 * TODO : Спецификация по WS mechanism
 * TODO : Спецификация по WS mechanism - поведение
 * TODO : Спецификация по WS mechanism - exceptions
 * TODO :
 * TODO :
 */
class TODO